/**
 * Utilidades SOLO para reservas
 */
export const formatearReserva = (reserva) => {
  // Formateo de datos de reserva
};

export const validarReserva = (reservaData) => {
  // Validaciones específicas de reserva
};