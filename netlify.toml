[build]
  base = "Frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--no-optional"

# Esta configuración resuelve el problema con @rollup/rollup-linux-x64-gnu
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200